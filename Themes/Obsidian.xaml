<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ═══════════════ OBSIDIAN PALETTE ═══════════════ -->
    <Color x:Key="VoidBlackColor">#FF05040A</Color>
    <Color x:Key="Void0Color">#FF08060E</Color>
    <Color x:Key="Void1Color">#FF0D0A16</Color>
    <Color x:Key="Void2Color">#FF13101F</Color>
    <Color x:Key="Void3Color">#FF1B1629</Color>
    <Color x:Key="Void4Color">#FF241D35</Color>
    <Color x:Key="StrokeColor">#FF2E2542</Color>
    <Color x:Key="StrokeSoftColor">#FF221B32</Color>

    <Color x:Key="PurpleColor">#FF8B3DFF</Color>
    <Color x:Key="PurpleDeepColor">#FF5B21B6</Color>
    <Color x:Key="PurpleGlowColor">#FFA855F7</Color>
    <Color x:Key="PurpleFadeColor">#FF3B1D63</Color>

    <Color x:Key="RedColor">#FFFF2E63</Color>
    <Color x:Key="RedDeepColor">#FFC1123F</Color>
    <Color x:Key="RedGlowColor">#FFFF5C7A</Color>

    <Color x:Key="BlueColor">#FF3B82F6</Color>
    <Color x:Key="BlueGlowColor">#FF38BDF8</Color>
    <Color x:Key="BlueDeepColor">#FF1D4ED8</Color>

    <Color x:Key="TextColor">#FFEDE7F6</Color>
    <Color x:Key="TextDimColor">#FF9C92B8</Color>
    <Color x:Key="TextMuteColor">#FF6A6182</Color>

    <SolidColorBrush x:Key="VoidBlack" Color="{StaticResource VoidBlackColor}"/>
    <SolidColorBrush x:Key="Void0" Color="{StaticResource Void0Color}"/>
    <SolidColorBrush x:Key="Void1" Color="{StaticResource Void1Color}"/>
    <SolidColorBrush x:Key="Void2" Color="{StaticResource Void2Color}"/>
    <SolidColorBrush x:Key="Void3" Color="{StaticResource Void3Color}"/>
    <SolidColorBrush x:Key="Void4" Color="{StaticResource Void4Color}"/>
    <SolidColorBrush x:Key="Stroke" Color="{StaticResource StrokeColor}"/>
    <SolidColorBrush x:Key="StrokeSoft" Color="{StaticResource StrokeSoftColor}"/>

    <SolidColorBrush x:Key="Purple" Color="{StaticResource PurpleColor}"/>
    <SolidColorBrush x:Key="PurpleDeep" Color="{StaticResource PurpleDeepColor}"/>
    <SolidColorBrush x:Key="PurpleGlow" Color="{StaticResource PurpleGlowColor}"/>
    <SolidColorBrush x:Key="Red" Color="{StaticResource RedColor}"/>
    <SolidColorBrush x:Key="RedDeep" Color="{StaticResource RedDeepColor}"/>
    <SolidColorBrush x:Key="RedGlow" Color="{StaticResource RedGlowColor}"/>
    <SolidColorBrush x:Key="Blue" Color="{StaticResource BlueColor}"/>
    <SolidColorBrush x:Key="BlueGlow" Color="{StaticResource BlueGlowColor}"/>

    <SolidColorBrush x:Key="Text" Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="TextDim" Color="{StaticResource TextDimColor}"/>
    <SolidColorBrush x:Key="TextMute" Color="{StaticResource TextMuteColor}"/>

    <!-- ═══════════════ GRADIENTS ═══════════════ -->
    <LinearGradientBrush x:Key="AppBackdrop" StartPoint="0,0" EndPoint="0.65,1">
        <GradientStop Color="#FF0A0713" Offset="0"/>
        <GradientStop Color="#FF120A20" Offset="0.35"/>
        <GradientStop Color="#FF16091C" Offset="0.68"/>
        <GradientStop Color="#FF08060E" Offset="1"/>
    </LinearGradientBrush>

    <RadialGradientBrush x:Key="PurpleBloom" GradientOrigin="0.5,0.5" Center="0.5,0.5" RadiusX="0.75" RadiusY="0.75">
        <GradientStop Color="#559D4EDD" Offset="0"/>
        <GradientStop Color="#00000000" Offset="1"/>
    </RadialGradientBrush>

    <RadialGradientBrush x:Key="RedBloom" GradientOrigin="0.5,0.5" Center="0.5,0.5" RadiusX="0.8" RadiusY="0.8">
        <GradientStop Color="#4CFF2E63" Offset="0"/>
        <GradientStop Color="#00000000" Offset="1"/>
    </RadialGradientBrush>

    <RadialGradientBrush x:Key="BlueBloom" GradientOrigin="0.5,0.5" Center="0.5,0.5" RadiusX="0.8" RadiusY="0.8">
        <GradientStop Color="#333B82F6" Offset="0"/>
        <GradientStop Color="#00000000" Offset="1"/>
    </RadialGradientBrush>

    <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource PurpleColor}" Offset="0"/>
        <GradientStop Color="#FFB13BE8" Offset="0.45"/>
        <GradientStop Color="{StaticResource RedColor}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="AccentGradientWide" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="{StaticResource BlueColor}" Offset="0"/>
        <GradientStop Color="{StaticResource PurpleColor}" Offset="0.42"/>
        <GradientStop Color="#FFD9308C" Offset="0.78"/>
        <GradientStop Color="{StaticResource RedColor}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PanelGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#FF161122" Offset="0"/>
        <GradientStop Color="#FF100C1B" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="SidebarGradient" StartPoint="0,0" EndPoint="0.4,1">
        <GradientStop Color="#FF130E20" Offset="0"/>
        <GradientStop Color="#FF0D0916" Offset="0.55"/>
        <GradientStop Color="#FF120A16" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PadGradient" StartPoint="0,0" EndPoint="0.8,1">
        <GradientStop Color="#FF1D1730" Offset="0"/>
        <GradientStop Color="#FF171126" Offset="0.6"/>
        <GradientStop Color="#FF14101F" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PadGradientHover" StartPoint="0,0" EndPoint="0.8,1">
        <GradientStop Color="#FF2E2050" Offset="0"/>
        <GradientStop Color="#FF261634" Offset="0.65"/>
        <GradientStop Color="#FF2A1424" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PadGradientActive" StartPoint="0,0" EndPoint="0.8,1">
        <GradientStop Color="#FF52219B" Offset="0"/>
        <GradientStop Color="#FF7A1E63" Offset="0.6"/>
        <GradientStop Color="#FF8E1436" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="TitleBarGradient" StartPoint="0,0" EndPoint="1,0.6">
        <GradientStop Color="#FF120C1F" Offset="0"/>
        <GradientStop Color="#FF180C22" Offset="0.5"/>
        <GradientStop Color="#FF14090F" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="RecordGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource RedGlowColor}" Offset="0"/>
        <GradientStop Color="{StaticResource RedDeepColor}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="MeterGradient" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="{StaticResource BlueColor}" Offset="0"/>
        <GradientStop Color="{StaticResource PurpleColor}" Offset="0.5"/>
        <GradientStop Color="{StaticResource RedColor}" Offset="0.92"/>
    </LinearGradientBrush>

    <!-- ═══════════════ EFFECTS ═══════════════ -->
    <DropShadowEffect x:Key="GlowPurple" Color="{StaticResource PurpleColor}" BlurRadius="22" ShadowDepth="0" Opacity="0.55"/>
    <DropShadowEffect x:Key="GlowRed" Color="{StaticResource RedColor}" BlurRadius="22" ShadowDepth="0" Opacity="0.6"/>
    <DropShadowEffect x:Key="GlowBlue" Color="{StaticResource BlueColor}" BlurRadius="18" ShadowDepth="0" Opacity="0.5"/>
    <DropShadowEffect x:Key="PanelShadow" Color="#FF000000" BlurRadius="26" ShadowDepth="5" Opacity="0.65"/>

    <!-- ═══════════════ TEXT STYLES ═══════════════ -->
    <Style x:Key="H1" TargetType="TextBlock">
        <Setter Property="FontSize" Value="19"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
    </Style>
    <Style x:Key="H2" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
    </Style>
    <Style x:Key="SectionLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10.5"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource TextMute}"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>
    <Style x:Key="Body" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style x:Key="Hint" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="{StaticResource TextMute}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- ═══════════════ SCROLLBAR ═══════════════ -->
    <Style x:Key="SbThumb" TargetType="Thumb">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="B" Background="#FF2E2542" CornerRadius="4" Margin="3,2"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="B" Property="Background" Value="{StaticResource Purple}"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="B" Property="Background" Value="{StaticResource Red}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="11"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="Transparent">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource SbThumb}"/>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter Property="Height" Value="11"/>
                            <Setter Property="Width" Value="Auto"/>
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════ BUTTONS ═══════════════ -->
    <Style x:Key="GhostButton" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="12,7"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="B" Background="#22FFFFFF" BorderBrush="{StaticResource Stroke}"
                            BorderThickness="1" CornerRadius="7" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="B" Property="Background" Value="#33A855F7"/>
                            <Setter TargetName="B" Property="BorderBrush" Value="{StaticResource Purple}"/>
                            <Setter Property="Foreground" Value="{StaticResource Text}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="B" Property="Background" Value="#44FF2E63"/>
                            <Setter TargetName="B" Property="BorderBrush" Value="{StaticResource Red}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="B" Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="Glow" CornerRadius="8" Background="{StaticResource AccentGradient}" Opacity="0"/>
                        <Border x:Name="B" Background="{StaticResource AccentGradient}" CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Glow" Property="Opacity" Value="0.85"/>
                            <Setter TargetName="Glow" Property="Effect" Value="{StaticResource GlowPurple}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="B" Property="Opacity" Value="0.75"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="B" Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
        <Setter Property="Foreground" Value="{StaticResource RedGlow}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="B" Background="#22FF2E63" BorderBrush="#66FF2E63"
                            BorderThickness="1" CornerRadius="7" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="B" Property="Background" Value="#55FF2E63"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="B" Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="IconButton" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Width" Value="28"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="B" Background="Transparent" CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="B" Property="Background" Value="#33A855F7"/>
                            <Setter Property="Foreground" Value="{StaticResource Text}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="B" Property="Background" Value="#44FF2E63"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TitleBarButton" TargetType="Button" BasedOn="{StaticResource IconButton}">
        <Setter Property="Width" Value="42"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
        <Setter Property="FontSize" Value="10"/>
    </Style>

    <!-- ═══════════════ INPUTS ═══════════════ -->
    <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="CaretBrush" Value="{StaticResource PurpleGlow}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource Purple}"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Padding" Value="9,7"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="B" Background="#FF0B0813" BorderBrush="{StaticResource Stroke}"
                            BorderThickness="1" CornerRadius="7">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="B" Property="BorderBrush" Value="#FF4A3A68"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="B" Property="BorderBrush" Value="{StaticResource Purple}"/>
                            <Setter TargetName="B" Property="Effect" Value="{StaticResource GlowPurple}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="B" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ComboToggle" TargetType="ToggleButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="B" Background="#FF0B0813" BorderBrush="{StaticResource Stroke}"
                            BorderThickness="1" CornerRadius="7">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="22"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0" Margin="9,0,0,0" VerticalAlignment="Center"/>
                            <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                                  Data="M 0 0 L 4 4 L 8 0" Stroke="{StaticResource TextDim}" StrokeThickness="1.6"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="B" Property="BorderBrush" Value="{StaticResource Purple}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="B" Property="BorderBrush" Value="{StaticResource Purple}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Height" Value="34"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="PART_ToggleBtn" Style="{StaticResource ComboToggle}" Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                            <ContentPresenter x:Name="CP" Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              TextBlock.Foreground="{StaticResource Text}"
                                              VerticalAlignment="Center" Margin="0,0,4,0"/>
                        </ToggleButton>
                        <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}" PopupAnimation="Fade" Focusable="False">
                            <Border Background="#FF141020" BorderBrush="{StaticResource Purple}" BorderThickness="1"
                                    CornerRadius="8" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="320"
                                    Margin="0,4,0,0" Effect="{StaticResource PanelShadow}">
                                <ScrollViewer>
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
        <Setter Property="Padding" Value="10,7"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="B" Background="Transparent" CornerRadius="5" Margin="4,2" Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="B" Property="Background" Value="#44A855F7"/>
                            <Setter Property="Foreground" Value="{StaticResource Text}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="B" Property="Background" Value="{StaticResource AccentGradient}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Border x:Name="Box" Width="17" Height="17" CornerRadius="5" Background="#FF0B0813"
                                BorderBrush="{StaticResource Stroke}" BorderThickness="1" VerticalAlignment="Center">
                            <Path x:Name="Tick" Data="M 2.5 6 L 5.5 9.5 L 11 2.5" Stroke="White" StrokeThickness="1.9"
                                  Opacity="0" StrokeEndLineCap="Round" StrokeStartLineCap="Round"/>
                        </Border>
                        <ContentPresenter Margin="9,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="{StaticResource AccentGradient}"/>
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Purple}"/>
                            <Setter TargetName="Tick" Property="Opacity" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource PurpleGlow}"/>
                            <Setter Property="Foreground" Value="{StaticResource Text}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Slider">
        <Setter Property="Height" Value="22"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid VerticalAlignment="Center">
                        <Border Height="5" CornerRadius="3" Background="#FF1B1629" VerticalAlignment="Center"/>
                        <Track x:Name="PART_Track">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="Slider.DecreaseLarge" Focusable="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="{StaticResource AccentGradient}" Height="5"
                                                    CornerRadius="3" VerticalAlignment="Center"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb x:Name="Th" Width="15" Height="15">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Grid>
                                                <Ellipse x:Name="Ring" Fill="White" Width="15" Height="15"/>
                                                <Ellipse Fill="{StaticResource AccentGradient}" Width="8" Height="8"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Ring" Property="Effect" Value="{StaticResource GlowPurple}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="Slider.IncreaseLarge" Opacity="0" Focusable="False"/>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════ CONTEXT MENU ═══════════════ -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="#FF141020"/>
        <Setter Property="BorderBrush" Value="{StaticResource Purple}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="#FF141020" BorderBrush="#FF3B2A5C" BorderThickness="1" CornerRadius="9"
                            Padding="5" Effect="{StaticResource PanelShadow}">
                        <ItemsPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="B" Background="Transparent" CornerRadius="6" Padding="11,7">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0" ContentSource="Header" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{TemplateBinding InputGestureText}" Margin="18,0,0,0"
                                       Foreground="{StaticResource TextMute}" FontSize="11" VerticalAlignment="Center"/>
                            <Path x:Name="Arrow" Grid.Column="2" Margin="12,0,0,0" Data="M 0 0 L 4 4 L 0 8"
                                  Stroke="{StaticResource TextMute}" StrokeThickness="1.4" Visibility="Collapsed"
                                  VerticalAlignment="Center"/>
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Right"
                                   IsOpen="{TemplateBinding IsSubmenuOpen}" Focusable="False">
                                <Border Background="#FF141020" BorderBrush="#FF3B2A5C" BorderThickness="1"
                                        CornerRadius="9" Padding="5" Margin="3,0,0,0" Effect="{StaticResource PanelShadow}">
                                    <ItemsPresenter/>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="B" Property="Background" Value="#44A855F7"/>
                            <Setter Property="Foreground" Value="{StaticResource Text}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Height="1" Background="{StaticResource StrokeSoft}" Margin="8,4"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ToolTip">
        <Setter Property="Foreground" Value="{StaticResource Text}"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="#FF17122A" BorderBrush="#FF3B2A5C" BorderThickness="1" CornerRadius="7"
                            Padding="10,6" Effect="{StaticResource PanelShadow}">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ═══════════════ TABS ═══════════════ -->
    <Style x:Key="VerticalTabItem" TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="B" Background="Transparent" CornerRadius="8" Margin="0,2" Padding="13,10">
                        <Grid>
                            <Border x:Name="Bar" Width="3" HorizontalAlignment="Left" CornerRadius="2"
                                    Background="{StaticResource AccentGradient}" Opacity="0" Margin="-8,2,0,2"/>
                            <ContentPresenter ContentSource="Header" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="B" Property="Background" Value="#22A855F7"/>
                            <Setter Property="Foreground" Value="{StaticResource Text}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="B" Property="Background" Value="#33A855F7"/>
                            <Setter TargetName="Bar" Property="Opacity" Value="1"/>
                            <Setter Property="Foreground" Value="{StaticResource Text}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
